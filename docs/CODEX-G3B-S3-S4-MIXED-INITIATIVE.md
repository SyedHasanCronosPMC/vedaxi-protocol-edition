# Elastic Web — G3B Mixed-Initiative Controls: S3/S4

## Entry condition

Do not begin this phase unless the deployed G3A visual acceptance check has been completed and recorded as:

```text
G3A = PASS
```

If `DECISIONS.md` does not contain that evidence, stop and report that G3A visual acceptance is still pending.

Current expected gate state before work:

```text
G1 = PARTIAL
G2 = PASS
G3A = PASS
```

G1 remains a separate target-WebMCP-host verification track.

---

# 1. Read before changing code

Read:

```text
D:\Github\Elastic Web\DECISIONS.md

D:\Github\Elastic Web\docs\build-spec\01-fixture-and-demo-states.md
D:\Github\Elastic Web\docs\build-spec\02-tool-manifest.md
D:\Github\Elastic Web\docs\build-spec\03-attention-policy.md
D:\Github\Elastic Web\docs\build-spec\04-architecture.md
D:\Github\Elastic Web\docs\build-spec\05-ui-motion-accessibility.md
```

Inspect the current implementation and preserve all G2/G3A invariants.

Do not redesign the engine.

---

# 2. Objective

Implement the smallest functional S3/S4 human-agent control layer proving:

```text
Agent finds conflict
      ↓
Human assigns authority
      ↓
System converts judgment into durable attention constraints
      ↓
Demoted content remains inspectable/restorable
      ↓
Undo restores prior state
```

This is the mixed-initiative and reversibility proof.

Do not begin final motion polish yet.

---

# 3. S3 — planted disagreement

Use the existing synthetic fixture exactly.

The authoritative disagreement is:

```text
Video segment 07:
~8% performance reduction for a 2°C rise above neutral

Paper Results:
4–11% measured effect

Paper Limitations:
34 participants
single climate zone
no broad generalization
```

Do not add extra scientific claims.

---

# 4. Surface the conflict

Ensure the functional UI can display the `compare_sources` result in the shared workspace.

Do not add a permanent chatbot panel.

The conflict should appear in or adjacent to the focal comparison content.

At minimum show:

```text
Paper
4–11% measured effect

Video
about 8%

Paper limitation
single climate zone · n=34 · no broad generalization
```

The exact wording must come from the synthetic fixture/result data.

---

# 5. Human authority action

Support the user instruction:

> Prioritize the peer-reviewed paper, but preserve the video's explanation.

Use the existing shared command/tool path and current authoritative pin semantics.

Expected conceptual operations:

```text
paper result → minimum FOCUSED
video.segment.07 → minimum CONTEXT
```

Follow the exact latest build-spec IDs and command shapes.

Do not implement this as a one-off React-only state change.

The action must update committed state.

---

# 6. Preserve is a constraint

Protect this invariant:

> **Preserve is a constraint, not a ranking preference.**

After the human instruction:

- paper evidence remains FOCUSED,
- video segment 07 cannot be silently demoted below CONTEXT,
- rationale shows that the state is pinned/protected.

The UI should visibly distinguish user authority from ordinary relevance scoring.

Use current rationale data rather than hardcoded UI-only explanation.

---

# 7. S3 rationale labels

Display the functional first-layer rationale.

Examples of the intended semantic meaning:

```text
↑ Pinned by you · protected from demotion

↑ Pinned at context · will not be demoted
```

Use the actual rationale generated by core state.

Do not invent separate duplicate rationale state in React.

---

# 8. S4 — Drawer and reversibility

Complete the functional drawer behavior.

Every demoted entry must have:

- label,
- provenance/origin where available,
- demotion reason,
- one-step restore control.

The minimum proof is:

```text
publisher/newsletter in drawer
→ Restore
→ relevant capability eligibility returns
→ Undo
→ prior state restored
```

Follow the exact current authoritative capability names.

---

# 9. Restore behavior

Restore must route through the existing shared application command/tool logic.

Do not directly mutate UI state.

The restore action must update:

```text
committed attention state
human render model
capability eligibility
rationale/history
best-effort surface derivation
```

If the current host does not immediately observe registration changes, that is not a blocker.

Eligibility remains authoritative.

---

# 10. Undo behavior

Expose an accessible human Undo control for the last reversible adaptation.

Undo must use the already-tested core undo implementation.

Verify:

- visual state restores,
- eligibility restores,
- capability indicator restores,
- rationale/history remains consistent.

Do not create a second UI-only undo mechanism.

---

# 11. Used-by-agent drawer category

If instrumentation already makes this trivial, include a functional:

```text
Used by agent
```

drawer category.

The purpose is auditability.

If adding it requires significant new infrastructure, do not expand scope; record it as deferred.

The primary S4 proof is restore + undo.

---

# 12. Capability indicator

Keep it derived.

S3 should preserve the authoritative capability count expected by the build spec.

Restoring publisher/newsletter content should update the derived count appropriately.

Undo should return it to the prior count.

Do not hardcode any count.

---

# 13. Tests

Preserve all existing tests.

Add deterministic tests for at least:

## A. Human authority / pinning

After S3 instruction:

- paper evidence has FOCUSED floor,
- video segment 07 has CONTEXT floor.

## B. Preserve constraint

A subsequent scoring event cannot demote the protected video segment below CONTEXT.

## C. Rationale provenance

Pinned elements expose rationale indicating human/user authority rather than ordinary score-only promotion.

## D. Restore

Restoring the selected drawer element updates committed state and eligibility.

## E. Undo after restore

Undo returns render and capability eligibility to the exact prior state.

## F. UI path

Human pin/restore/undo controls call shared application commands rather than local-only React state.

---

# 14. Accessibility baseline

Use real buttons.

Pin/restore/undo controls must be keyboard accessible.

Drawer controls must have meaningful labels.

Do not implement final reduced-motion behavior yet; final motion has not started.

---

# 15. No final animation yet

Keep state changes instant.

Do not add:

- final 1.4s choreography,
- easing curves,
- layout interpolation,
- stagger,
- decorative motion.

First prove S3/S4 correctness visibly.

---

# 16. What not to do

Do not:

- change G1 status
- fake host verification
- add backend/auth/database
- add extra agents
- add extra workflows
- add final motion
- redesign visual language
- rewrite working core architecture
- add LLM scoring
- hardcode S3/S4 in UI
- invent metrics

---

# 17. Manual verification

Verify deployed/local UI:

```text
S2
→ surface conflict
→ apply human authority instruction
→ S3 visibly shows pinned paper + preserved video
→ open drawer
→ restore selected publisher/newsletter item
→ capability indicator updates
→ undo
→ exact prior state returns
```

Record actual observations.

Deploy this functional checkpoint if practical.

---

# 18. Definition of G3B PASS

G3B PASS requires:

1. Conflict appears from fixture/tool result.
2. Human authority action routes through shared state command path.
3. Paper receives required FOCUSED floor.
4. Video segment 07 receives required CONTEXT floor.
5. Preserve survives subsequent scoring.
6. Rationale shows human authority.
7. Drawer entries are inspectable and restorable.
8. Restore updates state and eligibility.
9. Undo restores both UI and eligibility.
10. Capability indicator remains derived.
11. Existing tests still pass.
12. New S3/S4 tests pass.
13. Build passes.
14. No blocking lint errors.
15. Manual functional flow is visually verified.

If manual visual interaction cannot be verified in the current environment, report:

```text
G3B = PARTIAL
```

even if code-level tests pass.

---

# 19. Required report

## Changed

## Tests

## Manual functional verification

### S3

### S4 restore

### Undo

## G1

```text
PARTIAL — target WebMCP host verification pending
```

unless real evidence in `DECISIONS.md` now proves otherwise.

## G2

```text
PASS
```

## G3A

```text
PASS
```

## G3B

```text
PASS / PARTIAL / FAIL
```

## Risks

## Next

If G3B passes, recommend only the next smallest phase:

```text
G3C — motion + reduced-motion + keyboard/read-order polish
```

Do not begin G3C automatically.

Stop after reporting.
